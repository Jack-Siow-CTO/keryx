/// Historical dual-rail path — re-exports [MessagingShell] (ADR 0031).
///
/// Dual-rail Inbox + Sessions as peer permanent rails is superseded.
/// Prefer importing `messaging_shell.dart` directly.
library;

export 'messaging_shell.dart';
