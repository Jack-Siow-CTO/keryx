name: keryx_api
description: Pure Dart client for the Keryx Worker control plane (OpenAPI seam). No Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  http: ^1.2.2
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
  yaml: ^3.1.2
