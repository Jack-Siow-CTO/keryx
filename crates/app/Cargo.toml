[package]
name = "keryx-app"
description = "Keryx application services: agent loop, budgets, concurrency, cancel coordination"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
async-trait = "0.1"
keryx-domain = { path = "../domain" }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt", "sync", "time"] }
tokio-util = "0.7"
url = "2"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
